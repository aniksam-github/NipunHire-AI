Compare this already-parsed candidate profile with the job details. Return only the structured fields required by the response schema.

Every factor must name a concrete skill, qualification, or project. Give positive point contributions for demonstrated matches and negative contributions for gaps. Every factor needs a specific reason. The signed factor contributions must sum exactly to the overall match percentage. Never use vague factors such as "general fit".

Candidate profile:
{profile_json}

Job details:
{job_json}
