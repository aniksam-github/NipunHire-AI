Create a professional summary, key highlights, and a brief career snapshot from this already structured candidate profile. Return only the structured fields required by the response schema.

Candidate profile:
{profile_json}
