Evaluate this already-parsed candidate profile. Return only the structured fields required by the response schema. Assess the profile as presented and do not invent missing information.

Candidate profile:
{profile_json}
