Extract and categorize the skills evidenced by this already-parsed candidate profile. Return only the structured fields required by the response schema. Do not infer skills that are not supported by the profile.

Candidate profile:
{profile_json}
